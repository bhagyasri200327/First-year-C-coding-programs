#include<stdio.h>
void main()
{
    int x;
    scanf("%d",&x);
    int ans=checkevenodd(x);
    if(ans==0)
    printf("Even\n");
    else
    printf("odd\n");
}
int checkevenodd(int x)
{
    if(x%2==0)
    return(0);
    else
    return(1);
}
