#include<stdio.h>
#include<stdlib.h>
void main()
{
    int n,f;
    scanf("%d",&n);
    f=factorial(n);
    printf("\n%d",f);
}
int factorial(int n)
{
    int fact=1;
    while(n>=1)
    {
        fact=fact*n;
        n--;
    }
    return(fact);
}
