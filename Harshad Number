#include <stdio.h>

int main()
{
    int n,sum=0,r,x;
    scanf("%d",&n);
    x=n;
    while(n!=0)
    {
       r=n%10;
       sum=sum+r;
       n=n/10;
       
    }
    if(x%sum==0)
    printf("Harshad Number");
    else
    printf("Not Harshad number");
    return 0;
}
